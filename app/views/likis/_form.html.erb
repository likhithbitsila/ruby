<%= form_with(model: liki) do |form| %>
  <% if liki.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(liki.errors.count, "error") %> prohibited this liki from being saved:</h2>

      <ul>
        <% liki.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
